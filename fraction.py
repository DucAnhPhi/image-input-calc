class Fraction:
    def __init__(self, nominator, denominator, bar):
        # nominator and denominator are list of contour objects (see contour.py)
        self.nominator = nominator
        self.denominator = denominator
        # bar is a contour object (see contour.py)
        self.bar = bar
